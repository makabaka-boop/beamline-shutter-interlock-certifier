server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 单页应用：静态资源直接返回，其余路径回落到 index.html。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
